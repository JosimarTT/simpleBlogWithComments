mixin blogPost(post)
  div.span6
    a(href="/post/#{post._id}")
      h3 #{post.subject}
    p #{post.body.substr(0, 250) + '...'}
    p#info 
      div.tags
        for tag in post.tags
          strong 
            a(href="#") #{tag} 
      div.post-time
        em #{moment(post.created).format('LLL')}
    p
      a(class="btn btn-small",href="/post/#{post._id}") Seguir leyendo &raquo;  


div.hero-unit 
  div.container
          div.content
              div.row
                  div.span2
                      .image-block
                        img.className#IdName(src='/img/cofradia.png')
                  div.span10
                      H1 COFRADIA
                      H2 Hermandad fundada en el 2017
                      p Bienvenido a mi blog, ven y comparte tus ideas junto a mí...

!=partial('alert', flash)


 

div
  - for (var i = 0; i < postList.length; i++)
    div.row
      mixin blogPost(postList[i])
      - if (i + 1 < postList.length)
        mixin blogPost(postList[++i])

